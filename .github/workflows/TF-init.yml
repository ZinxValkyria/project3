name: Terraform init

on:
  workflow_run:
    workflows: ["AWS Infrastructure Deployment"]
    types:
      - completed

jobs:
  terraform-apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Initialize Terraform
        run: terraform init
        working-directory: ./terraform
